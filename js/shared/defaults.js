twtGitDefaults = {
  templates: {
    tweet_repository_template: "{{repository.description}}",
    tweet_issue_template: "{{repository.description}} issue {{issue.title}}",
    tweet_pr_template: "{{repository.description}} PR {{pr.title}}",
    tweet_release_template: "{{repository.title}} release: {{release.title}}"
  }
};